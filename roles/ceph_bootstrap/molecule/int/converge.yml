---
- name: Converge - init cluster
  hosts: ceph-mon1
  become: true
  vars:
    fail_on_service_errors: false
  tasks:
    - name: "Include bassinator.ceph"
      ansible.builtin.include_role:
        name: "einfachit.hpc.ceph"
